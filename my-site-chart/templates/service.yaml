apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-my-site
spec:
  type: {{ .Values.service.type }}
  selector:
    app: my-site
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      nodePort: {{ .Values.service.nodePort }}
